# Session log


Writing the firs longt paragraph to see the live preview behave while typing. It has **bold**, *italic*, `inline code`, and a [link](https://example.com) in it.

## What I tried today

- first list item edited
- - second item with **bold**
- - [ ] an open task
- - [x] a done task
> [!note] A callout header
> > body of the callout with $x^2$ math and ==marked==
>```js
>const a = 1;
>console.log(a);
>```
After the fence. Yes.

### Exit tests

- item one
- item two
after bullets

> quote one
> quote two
after quote

1. first
2. second
after ordered

- [x] task a
- [ ] task b
after tasks

### Table
 Two
| Col A | Col B |
|---|---|
| #1one | two x  |
| #2Xthree | four#3 |
| #4| |

Below the table.

This is the long paragraph of the session, typed one key at a time to see whether the preview stays steady under sustained input. It has a comma, a semicolon; a colon: quotes "like this" and 'like that', numbers such as 1,234.56 and 2026-09-08, a question? An exclamation! Parentheses (with content) and brackets [like these] plus a slash/and a backslash. It goes on for a while so that the line wraps several times inside the page, and then it stops. 東京 🍣 café


- tab test
plain para
- shift tab test
## Free writing

A second stretch of prose to fill the session with ordinary typing, the kind a person does when drafting notes rather than testing an editor. Short sentences. Some longer ones that run on a bit, with a few commas, to make the wrapping do some work.

1. step one
2. step two
3. step three
Closing paragraph after the ordered list.
s
| Name | Qty |
|---|---|
| apple | 3 |
| pear | 5 |

End with **bold** done.

## Slower drafting

Now a slower stretch, closer to real drafting speed, where I write a sentence, go back a few words, fix one, and carry on. The editor should not flicker or jump while the cursor moves through rendered inline marks like *this* or `that`. Sentences keep coming for a while. Here is a clause with a typo: teh quick brown fox. And here is where I fix it.


## Closing

The last stretch is just writing. Nothing clever, no tables, no lists, only sentences typed at something like a real pace so the editor has to keep up with a person rather than a script. If anything stutters, drops a character, or moves the view without being asked, this is where it would show. So far the text has arrived as typed, the preview has kept pace, and the file on disk has matched the keystrokes every time it was checked. A second paragraph of the closing, with an *emphasised* phrase and a `code span` in the middle, and a sentence that ends on a number like 42. Then a final line to say that the session is done.

### Enter x3 test

- alpha
- beta

gamma paragraph

- one
  two after backspace
  
  > quoted

after quote x3

1. first
   after ordered backspace
   
   - [ ] task
         after task backspace
         
         - x
         after two backspaces
         
         A last paragraph, typed slowly, about what it felt like to use the editor for half an hour. The good part is that the text and the file never disagreed: every time the file was read back, it held exactly the bytes that had been typed, in the right order, with the right padding around table cells. The rough parts were all about editing gestures rather than data: pressing Enter twice to leave a list, pressing Tab to indent, pressing Backspace to cancel a bullet, and arrowing into a table that sits below the fold. None of those lost text, but each one made the page do something other than what the hands expected, and that is the moment the illusion breaks.
