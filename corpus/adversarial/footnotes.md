<!-- adversarial: footnotes referenced, defined out of order, duplicated, undefined, and holding blocks -->
Ordinary text with a note[^first] and another[^second], then the first again[^first].

A note nobody defined[^missing] stays as it is written.

An empty label [^] and an unclosed one [^open are not references either.

[^second]: Defined before the one referenced first, so the numbers follow the text.

[^first]: A note with more than a sentence.

    A second paragraph, indented four spaces.

    - and a list
    - inside the note

[^third]: Never referenced anywhere above.
