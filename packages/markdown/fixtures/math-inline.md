Euler: $e^{i\pi} + 1 = 0$ and Pythagoras $a^2 + b^2 = c^2$.

Money: $5 and $10 stay prose, so do $20,000 and $30,000.

Digits after: $x$1 is not a closer, so the run ends at the next valid $x$ and $x$. is fine.

Spaces: $ not math$ and $not math $ and $ neither $.

Escaped: \$5 and \$10, and $a \$ b$ keeps its escaped dollar.

Display in flow: text $$\sum_{i=1}^n i$$ more text and $$ spaced $$ too.

Markdown inside math is literal: $a*b*c$ and $a_1 + b_2$ and $`x`$.

Multi-line: $a +
b$ closes across the line break.

Empty pair: $$$$ alone.

Triple: $$$ alone.

Lone: $$ alone.
