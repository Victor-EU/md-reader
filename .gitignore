.DS_Store
node_modules/
target/
dist/
*.tsbuildinfo
apps/desktop/src-tauri/gen/schemas/
.vitest/
.env
# Review pages written into the checkout by a review run. They are about
# the repository rather than part of it, and one was committed by
# accident once.
markdown-review-*.html
md-reader-review-*.html

# pdf.js data, copied out of node_modules at build time (ADR 0035).
apps/desktop/public/pdfjs/
